@model GestionNotasCunor.Models.actividad

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4>Actividad</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @*<div class="form-group">
            @Html.Label("Catedrático", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("id_catedratico", (SelectList)ViewBag.lista_catedraticos, new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Carrera", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select id="carreras" class="form-control">

                </select>
            </div>
        </div>*@

        <div class="form-group">
            @Html.Label("Curso", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("id_asign_curso", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.id_asign_curso, "", new { @class = "text-danger" })
            </div>
            @*<div class="col-md-10">
                <select id="cursos" class="form-control" ></select>
            </div>*@
        </div>

        <div class="form-group">
            @Html.Label("Actividad", new { @class = "control-label col-md-2" })
            @*@Html.LabelFor(model => model.nom_actividad, htmlAttributes: new { @class = "control-label col-md-2" })*@
            <div class="col-md-10">
                @Html.EditorFor(model => model.nom_actividad, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.nom_actividad, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Valor", new { @class = "control-label col-md-2" })
            @*@Html.LabelFor(model => model.valor, htmlAttributes: new { @class = "control-label col-md-2" })*@
            <div class="col-md-10">
                @Html.EditorFor(model => model.valor, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.valor, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Fecha", new { @class = "control-label col-md-2" })
            @*@Html.LabelFor(model => model.fecha, htmlAttributes: new { @class = "control-label col-md-2" })*@
            <div class="col-md-10">
                @Html.EditorFor(model => model.fecha, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.fecha, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default"/>
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Lista de actividades", "Index")
</div>

@*<script>
    $(function () {
        $("#id_catedratico").change(function () {
            $.get("/Actividades/busqCarrera", { idCatedratico: $("#id_catedratico").val() }, function (data) {
                $("#carreras").empty();
                $.each(data, function (index, row) {
                    $("#carreras").append("<option value ='" + row.id_carrera + "' >" + row.nom_carrera + "</option>")
                });
            });
        });
    });
</script>

<script>
    $(function () {
        $("#carreras").change(function () {
            $.get("/Actividades/busqCurso", { idCatedratico: $("#id_catedratico").val(), idCarrera: $("#carreras").val() }, function (data) {
                $("#cursos").empty();
                $.each(data, function (index, row) {
                    $("#cursos").append("<option value ='" + row.id_curso + "' >" + row.nom_curso + " </option>")
                });
            });
        });
    });
</script>*@